/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.discordapp.com', 'wizardeo.es', 'wizardeo.eu', `qiasvyigtpxlonlmrais.supabase.co`],
      },
};

export default nextConfig;